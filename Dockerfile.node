FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev
COPY dist/ dist/
COPY scripts/ scripts/
RUN mkdir -p /data
ENV CLAUDE_PLUGIN_DATA=/data
EXPOSE 3000
CMD ["node", "dist/index.js"]
